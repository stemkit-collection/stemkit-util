#!/bin/ksh

unset FOLDERS 
OPTERR=0

while getopts "vd:" option; do
  case "${option}" in
    v)
      set -xv
    ;;
    d)
      FOLDERS="${FOLDERS:+${FOLDERS} }${OPTARG}"
    ;;
    *)
      echo "USAGE `basename ${0}` [-v] [[-d<folder>] ...] [<svn command>]" 1>&2
      exit 2
    ;;
  esac
done
shift $(( ${OPTIND} - 1 ))

find ${FOLDERS:-.} -name src -prune -print | xargs -n1 svn "${@:-status}"
